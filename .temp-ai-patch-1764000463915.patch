```diff
--- a/src/components/file-explorer/FileExplorerContextMenu.tsx
+++ b/src/components/file-explorer/FileExplorerContextMenu.tsx
@@ -319,13 +319,13 @@
             backgroundColor: theme.palette.background.paper,
             borderColor: theme.palette.divider,
           }}
-          onClick={(e) => e.stopPropagation()} // Prevent closing when clicking on menu itself
+          // Removed inline onClick stopPropagation to prevent conflict with Paper onMouseDown
         >
         <Paper
             elevation={1}
             sx={{
                     bgcolor: theme.palette.background.default,
                 }}
+            onMouseDown={(e) => e.stopPropagation()} // Prevent closing via outside click listener when interacting with the menu
             >
             <Box
                 sx={{
```