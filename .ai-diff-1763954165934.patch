--- a/src/components/file-explorer/FileExplorerContextMenu.tsx
+++ b/src/components/file-explorer/FileExplorerContextMenu.tsx
@@ -34,6 +34,7 @@
 import { fileExplorerService } from './api/fileExplorerService';
 import { VIDEO_MIME_TYPES, AUDIO_MIME_TYPES } from '@/constants'; // NEW
 
+// ADDED: Import snackbar service
+import { snackbarService } from '@/services/snackbarService';
 
 /**
  * Helper function to handle clipboard copy action.
@@ -279,15 +280,11 @@
     });
 
     if (pathsAddedCount === 0) {
-        await dialogService.alert({
-            title: 'Scan Paths Unchanged',
-            content: 'All selected paths were already present in the AI scan configuration.',
-            maxWidth: 'xs'
-        });
+        // Use Snackbar for non-critical information feedback
+        snackbarService.info('All selected paths were already present in the AI scan configuration.');
         return;
     }
 
     // Convert Set back to comma-separated string and update store
     const newScanPathsInput = Array.from(existingPaths).join(', ');
     setScanPathsInput(newScanPathsInput);
 
-    await dialogService.alert({
-        title: 'Scan Paths Updated',
-        content: pathsAddedCount === 1 
-            ? `1 path added to AI scan configuration.`
-            : `${pathsAddedCount} paths added to AI scan configuration.`, 
-        maxWidth: 'xs'
-    });
+    const message = pathsAddedCount === 1 
+        ? `1 path added to AI scan configuration.`
+        : `${pathsAddedCount} paths added to AI scan configuration.`;
+        
+    // Use Snackbar for success feedback
+    snackbarService.success(`Scan Paths Updated: ${message}`);
     
 }, [entries, handleClose]);
 
