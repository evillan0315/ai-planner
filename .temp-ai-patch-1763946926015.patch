--- a/src/git/git.service.ts
+++ b/src/git/git.service.ts
@@ -242,12 +242,12 @@
       txt = txt.replace(/
/g, '\n');
 
-      // Ensure trailing newline
-      if (!txt.endsWith('\n')) txt += '\n';
+      // Aggressively trim leading/trailing whitespace/newlines
+      txt = txt.trim();
+      
+      // Ensure trailing newline if content exists (often required by git apply)
+      if (txt.length > 0 && !txt.endsWith('\n')) txt += '\n';
 
       return txt;
     };
 
     const sanitized = sanitizePatch(patchContent);
     const tempPatchPath = path.join(effectiveRoot, `.temp-ai-patch-${Date.now()}.patch`);
 
     try {
-      await fs.promises.writeFile(tempPatchPath, patchContent, 'utf-8');
+      // FIX: Write the sanitized content, not the raw input
+      await fs.promises.writeFile(tempPatchPath, sanitized, 'utf-8');
     } catch (writeErr: any) {