### Example Prompts for React (CDN) + Tailwind (CDN)

1. **“Create a simple React counter app using React CDN and Tailwind CDN. Produce a single complete HTML file that I can open directly in the browser.”**

2. **“Build a responsive navigation bar in React with Tailwind, using only CDN imports. Output one full HTML file with React, ReactDOM, Babel, and Tailwind loaded via CDN.”**

3. **“Generate an HTML file that renders a React todo list with add/remove functionality. Use React via CDN and style it using Tailwind CDN.”**

4. **“Make a React modal component styled with Tailwind. Provide the full standalone HTML that runs without a build.”**

5. **“Give me a single-file React+Tailwind dashboard layout with a sidebar, header, and content area. Must work with CDN imports only.”**

6. **“Produce an example React form with validation (email + password) styled using Tailwind utilities. Use React and Babel CDNs and return the full HTML file.”**

7. **“Create a dark mode toggle in React, styled with Tailwind classes, using CDN scripts only. Return one complete browser-ready HTML file.”**

8. **“Write a React accordion/FAQ component using Tailwind for styling. Deliver the full HTML file using CDN links for React, ReactDOM, Babel, and Tailwind.”**

9. **“Make a simple React image gallery grid using Tailwind for layout. Use only CDN imports and provide a single standalone HTML file.”**

10. **“Create a React tab component using Tailwind for styling. Provide the entire HTML document that works directly in the browser without any bundlers.”**

11. **“Build a React dropdown menu with Tailwind transition classes, using React via CDN and JSX via Babel CDN. Output the full HTML.”**

12. **“Generate a landing page hero section in React using Tailwind classes. Include React, ReactDOM, Babel, and Tailwind via CDN inside one file.”**

13. **“Create a React component that fetches data from a public API and displays it in a Tailwind-styled card layout. Must be a complete HTML file using CDNs.”**

14. **“Write a React timer/pomodoro UI styled with Tailwind. Provide the whole HTML file runnable with no build step.”**

15. **“Give me a React component that animates elements using Tailwind classes (fade/scale). Only use CDN imports and return the full browser-ready HTML.”**

16. **“Build a React multi-step form wizard with Tailwind styling. Output one fully working HTML file with React, Babel, and Tailwind via CDN.”**

17. **“Create a responsive React price table using Tailwind, using CDN imports only. Provide a complete HTML file.”**

18. **“Produce a React sidebar that collapses/expands, styled with Tailwind, and deliver it in a single HTML file that runs without compiling.”**

19. **“Make a React toast notification component with Tailwind styles. Return the full browser-ready HTML file using CDN scripts.”**

20. **“Create a React draggable card component (basic) using CDN React + Tailwind classes. Provide one complete standalone HTML file.”**

---
