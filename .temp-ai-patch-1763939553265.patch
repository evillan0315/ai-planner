--- a/../../src/planner/validator.ts
+++ b/../../src/planner/validator.ts
@@ -107,10 +107,11 @@
     title: z.string().min(5, "title must be at least 5 characters"),
     summary: z.string().min(10, "summary must be at least 10 characters"),
     thoughtProcess: z
-      .array(z.string().min(3))
-      .min(1, "thoughtProcess requires at least one bullet")
-      .max(6, "thoughtProcess cannot have more than 6 bullets"),
+      .union([
+        z.string().min(3, "thoughtProcess string must be at least 3 characters"),
+        z.array(z.string().min(3)).min(1, "thoughtProcess array requires at least one bullet").max(6, "thoughtProcess cannot have more than 6 bullets"),
+      ]),
     // assumptions required by earlier schema; allow empty array
     assumptions: z.array(z.string()).min(0).default([]),
     confidence: z.number().min(0).max(1),
