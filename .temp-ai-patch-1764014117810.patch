--- a/src/components/editor/views/FileContentRenderer.tsx
+++ b/src/components/editor/views/FileContentRenderer.tsx
@@ -3,7 +3,6 @@
 import {
   Box,
   Typography,
-  CircularProgress,
   Alert,
   SxProps,
 } from '@mui/material';
@@ -21,6 +20,9 @@
 import AudioPlayer from '@/components/ui/player/AudioPlayer';
 import VideoPlayer from '@/components/ui/player/VideoPlayer';
 
+// ADDED LOADING COMPONENT
+import Loading from '@/components/Loading'; 
+
 // --- Types ---
 
 type EditorContentSource = (IEditorContent | IEditorTab | IWindowContent);
@@ -107,9 +109,11 @@
     if (isLoading) {
          return (
              <Box className="flex flex-col items-center justify-center h-full">
-                 <CircularProgress />
-                <Typography variant="h6" sx={{ mt: 2 }} color="text.secondary">
+                 {/* REPLACED: CircularProgress with Loading type="linear" */}
+                 <Loading 
+                    type="linear" 
+                    message={`Loading ${fileEntry?.name || 'Content'}...`}
+                    className="w-full"
+                 />
+                <Typography variant="h6" sx={{ mt: 2 }} color="text.secondary" className="mt-3">
                     Loading {fileEntry?.name || 'Content'}...
                 </Typography>
              </Box>
@@ -140,9 +144,11 @@
 
         if (mediaUrlLoading) {
             return (
                 <Box className="flex justify-center items-center h-full">
-                    <CircularProgress />
-                    <Typography sx={{ml: 2}} color="text.secondary">Generating secured media stream URL...</Typography>
+                    {/* REPLACED: CircularProgress with Loading type="linear" */}
+                    <Loading 
+                        type="linear" 
+                        message="Generating secured media stream URL..." 
+                        barHeight={8}
+                        skeletonWidth="200px"
+                    />
                 </Box>
             );
         }
